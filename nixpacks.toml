[phases.setup]
nixPkgs = ["ffmpeg", "cairo", "pango", "pkg-config", "gcc", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "fastapi run main.py --host 0.0.0.0 --port $PORT"
